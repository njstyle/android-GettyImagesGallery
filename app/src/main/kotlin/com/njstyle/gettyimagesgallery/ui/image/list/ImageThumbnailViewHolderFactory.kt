package com.njstyle.gettyimagesgallery.ui.image.list

import android.view.ViewGroup

class ImageThumbnailViewHolderFactory {
    
    fun createViewHolder(parent: ViewGroup) = ImageThumbnailViewHolder(parent)
}